apiVersion: apps/v1
kind: Deployment
metadata:
  name: data-pipeline-failure-rca-agent
spec:
  replicas: 1
  selector:
    matchLabels:
      app: data-pipeline-failure-rca-agent
  template:
    metadata:
      labels:
        app: data-pipeline-failure-rca-agent
    spec:
      securityContext:
        runAsNonRoot: true
      containers:
        - name: agent
          image: REPLACE_WITH_REGISTRY/data-pipeline-failure-rca-agent:0.1.0
          args: ["doctor"]
          securityContext:
            allowPrivilegeEscalation: false
            readOnlyRootFilesystem: true
          resources:
            requests:
              cpu: "100m"
              memory: "128Mi"
            limits:
              cpu: "500m"
              memory: "512Mi"
